"""
QUICK REFERENCE - Testing Individual Subtasks
"""

# INDEPENDENT TESTS - Run each independently to test specific components

# 1. Test UART Communication (verify commands sent correctly)
# Command: python test_uart_handler.py
# Tests: Connection, message formats, send_command(), send_capture()
# Needs: UART device/RPi connected (will fail gracefully if not)
# Output: Verifies ANGLE:X,SPEED:Y and CAPTURE:X,Y formats

# 2. Test Camera Interface (verify USB camera works)
# Command: python test_camera.py
# Tests: Camera detection, frame capture, FPS, color conversion, ROI
# Needs: USB camera connected
# Output: Resolution, FPS, frame properties

# 3. Test Direction Calculation (verify mouse-to-angle conversion)
# Command: python test_direction.py
# Tests: Center=0speed, angles, speed scaling, capping at 1.0
# Needs: Nothing (pure math)
# Output: Angle and speed for various mouse positions

# 4. Test GUI Components (verify window and layout)
# Command: python test_gui.py
# Tests: Window creation, camera detection, direction computation
# Needs: Display server (optional display test)
# Output: GUI properties and optional 5-second preview

# RUN ALL TESTS
# Command: python run_tests.py
# Output: Summary of all test results


"""
FILE ORGANIZATION - Simplified but Modular
"""

# MAIN APPLICATION (3 files for core functionality)
main.py                    # Entry point - config changed to UART settings
arm_gui.py                # Alternative entry point - UART params
camera_gui.py             # Main GUI + camera + UART integration

# UART HANDLER (single file for all UART communication)
uart_handler.py           # UartHandler class (was socket_handler.py)

# INDEPENDENT TESTS (4 files, each tests one subsystem)
test_uart_handler.py      # Tests UART communication only
test_camera.py            # Tests camera detection & capture only
test_direction.py         # Tests direction/speed math only
test_gui.py               # Tests GUI components only

# TEST RUNNERS
run_tests.py              # Master test runner - runs all tests
TESTING_GUIDE.md          # Comprehensive testing documentation


"""
SUBTASK TESTING - What Each Test Verifies
"""

TEST 1: UART HANDLER (test_uart_handler.py)
├─ Initialization: UART object created with port & baudrate ✓
├─ Connection: serial port opened successfully ✓
├─ Command sending: ANGLE:X,SPEED:Y format ✓
├─ Capture sending: CAPTURE:X,Y format ✓
├─ Message formats: Validated structure ✓
└─ Disconnect: Port closed properly ✓

TEST 2: CAMERA INTERFACE (test_camera.py)
├─ Detection: Finds available USB cameras ✓
├─ Properties: Gets resolution, FPS, brightness, contrast ✓
├─ Frame capture: Reads frames from camera ✓
├─ Frame rate: Measures actual FPS ✓
├─ Color conversion: BGR → RGB works ✓
├─ ROI extraction: Crops detected regions ✓
└─ Multi-camera: Can switch between cameras ✓

TEST 3: DIRECTION CALCULATION (test_direction.py)
├─ Center position: Speed = 0 when mouse at center ✓
├─ Cardinal directions: 0°, 90°, 180°, 270° ✓
├─ Diagonal directions: 45° increments ✓
├─ Speed scaling: Increases with distance ✓
├─ Speed capping: Never exceeds 1.0 ✓
├─ All quadrants: All directions work ✓
└─ Message format: UART format generation ✓

TEST 4: GUI (test_gui.py)
├─ Label creation: CameraLabel widget works ✓
├─ GUI initialization: CameraGUI window created ✓
├─ Camera integration: Detected cameras shown ✓
├─ Components: Image label, dropdown visible ✓
├─ Direction compute: Math integration ✓
├─ ROI calculation: Capture region computed ✓
└─ Optional display: Visual verification ✓


"""
FAILURE DIAGNOSIS FLOWCHART
"""

Application not working? Use tests to find the problem:

START
  ↓
Does math look correct?
  → NO: Run test_direction.py
         Fix angle/speed calculations
  ↓YES
Is camera showing?
  → NO: Run test_camera.py
         Check USB connection
         Check camera detection
  ↓YES
Are commands going to RPi?
  → NO: Run test_uart_handler.py
         Check COM port
         Check baud rate
         Check RPi connection
  ↓YES
GUI working?
  → NO: Run test_gui.py
         Check PyQt5 installation
  ↓YES
✓ Everything is working!


"""
CONFIGURATION LOCATIONS
"""

UART Settings (change these for your hardware):
├─ main.py
│  ├─ RPi_PORT = 'COM3'        # Change to your COM port
│  └─ RPi_BAUDRATE = 9600      # Match your RPi baud rate
├─ arm_gui.py
│  ├─ RPi_PORT = 'COM3'        
│  └─ RPi_BAUDRATE = 9600      
└─ camera_gui.py
   ├─ rpi_port parameter
   └─ rpi_baudrate parameter

Camera Settings (in camera_gui.py):
├─ detect_cameras() range(5)    # Checks cameras 0,1,2,3,4
└─ DEFAULT RESOLUTION: 640x480  # OpenCV auto-selects

Direction Calculation (in camera_gui.py):
├─ ROI size = 60 pixels
└─ Speed capped at 1.0


"""
EXPECTED MESSAGES BETWEEN SYSTEMS
"""

FROM LAPTOP → TO RPi (via UART):
  ANGLE:1.57,SPEED:0.75\n      # Continuous direction update
  CAPTURE:320,240\n             # Capture command (K key)

FROM RPi → TO MOTOR CONTROLLER:
  (Your RPi firmware handles this)


"""
START TESTING NOW
"""
# Step 1: Test math without hardware
python test_direction.py

# Step 2: Test camera detection
python test_camera.py

# Step 3: Test UART communication
python test_uart_handler.py

# Step 4: Test GUI integration
python test_gui.py

# Step 5: Run all tests
python run_tests.py

# Step 6: Run main application
python main.py
